plugins {
    id 'java'
}

// O grupo do projeto raiz deve ser o seu namespace verificado
group = 'io.github.nicolascunha'
version = '1.0.0' // Evite SNAPSHOT para publicação inicial no Central

repositories {
    mavenCentral()
}

subprojects {
    apply plugin: 'maven-publish'
    apply plugin: 'java-library'
    apply plugin: 'signing'

    // Garante que todos os módulos (core, strings, etc) herdem o grupo correto
    group = 'io.github.nicolascunha'
    version = '1.0.0'

    java {
        withJavadocJar()
        withSourcesJar()
    }

    publishing {
        publications {
            mavenJava(MavenPublication) {
                from components.java

                // IMPORTANTE: Deve ser exatamente o namespace verificado no Sonatype
                groupId = 'io.github.nicolascunha'
                artifactId = project.name
                version = '1.0.0'

                pom {
                    name = "SJL - ${project.name}"
                    description = 'Simple Java Library modular components'
                    url = 'https://github.com/NicolasCunha/sjl'
                    licenses {
                        license {
                            name = 'The Apache License, Version 2.0'
                            url = 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                        }
                    }
                    developers {
                        developer {
                            id = 'nicolascunha'
                            name = 'Nicolas Filipe Cunha'
                        }
                    }
                    scm {
                        connection = 'scm:git:git://github.com/NicolasCunha/sjl.git'
                        developerConnection = 'scm:git:ssh://github.com/NicolasCunha/sjl.git'
                        url = 'https://github.com/NicolasCunha/sjl'
                    }
                }
            }
        }
        repositories {
            maven {
                name = "OSSRH"
                url = "https://s01.oss.sonatype.org/service/local/staging/deploy/maven2/"
                credentials {
                    username = System.getenv("MAVEN_USERNAME")
                    password = System.getenv("MAVEN_PASSWORD")
                }
            }
        }
    }

    signing {
        def signingKey = System.getenv("GPG_SIGNING_KEY")
        def signingPassword = System.getenv("GPG_PASSPHRASE")

        // Só tenta assinar se as chaves estiverem presentes (evita erro em build local)
        if (signingKey != null && signingPassword != null) {
            useInMemoryPgpKeys(signingKey, signingPassword)
            sign publishing.publications.mavenJava
        }
    }
}

// Configuração de testes no nível raiz se necessário
test {
    useJUnitPlatform()
}